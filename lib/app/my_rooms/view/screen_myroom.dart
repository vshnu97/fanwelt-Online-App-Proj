import 'package:flutter/material.dart';

class ScreenMyRooms extends StatelessWidget {
  const ScreenMyRooms({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
body: Center(child: Text('My room'),),
    );
    
  }
}